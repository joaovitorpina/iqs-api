// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"endereco/ent/cep"
	"endereco/ent/cidade"
	"endereco/ent/endereco"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// CepCreate is the builder for creating a Cep entity.
type CepCreate struct {
	config
	mutation *CepMutation
	hooks    []Hook
}

// SetLogradouro sets the "logradouro" field.
func (cc *CepCreate) SetLogradouro(s string) *CepCreate {
	cc.mutation.SetLogradouro(s)
	return cc
}

// SetBairro sets the "bairro" field.
func (cc *CepCreate) SetBairro(s string) *CepCreate {
	cc.mutation.SetBairro(s)
	return cc
}

// SetID sets the "id" field.
func (cc *CepCreate) SetID(i int32) *CepCreate {
	cc.mutation.SetID(i)
	return cc
}

// SetCidadeID sets the "cidade" edge to the Cidade entity by ID.
func (cc *CepCreate) SetCidadeID(id int) *CepCreate {
	cc.mutation.SetCidadeID(id)
	return cc
}

// SetCidade sets the "cidade" edge to the Cidade entity.
func (cc *CepCreate) SetCidade(c *Cidade) *CepCreate {
	return cc.SetCidadeID(c.ID)
}

// AddEnderecoIDs adds the "enderecos" edge to the Endereco entity by IDs.
func (cc *CepCreate) AddEnderecoIDs(ids ...int) *CepCreate {
	cc.mutation.AddEnderecoIDs(ids...)
	return cc
}

// AddEnderecos adds the "enderecos" edges to the Endereco entity.
func (cc *CepCreate) AddEnderecos(e ...*Endereco) *CepCreate {
	ids := make([]int, len(e))
	for i := range e {
		ids[i] = e[i].ID
	}
	return cc.AddEnderecoIDs(ids...)
}

// Mutation returns the CepMutation object of the builder.
func (cc *CepCreate) Mutation() *CepMutation {
	return cc.mutation
}

// Save creates the Cep in the database.
func (cc *CepCreate) Save(ctx context.Context) (*Cep, error) {
	var (
		err  error
		node *Cep
	)
	if len(cc.hooks) == 0 {
		if err = cc.check(); err != nil {
			return nil, err
		}
		node, err = cc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*CepMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = cc.check(); err != nil {
				return nil, err
			}
			cc.mutation = mutation
			if node, err = cc.sqlSave(ctx); err != nil {
				return nil, err
			}
			mutation.id = &node.ID
			mutation.done = true
			return node, err
		})
		for i := len(cc.hooks) - 1; i >= 0; i-- {
			if cc.hooks[i] == nil {
				return nil, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = cc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, cc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (cc *CepCreate) SaveX(ctx context.Context) *Cep {
	v, err := cc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (cc *CepCreate) Exec(ctx context.Context) error {
	_, err := cc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (cc *CepCreate) ExecX(ctx context.Context) {
	if err := cc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (cc *CepCreate) check() error {
	if _, ok := cc.mutation.Logradouro(); !ok {
		return &ValidationError{Name: "logradouro", err: errors.New(`ent: missing required field "Cep.logradouro"`)}
	}
	if v, ok := cc.mutation.Logradouro(); ok {
		if err := cep.LogradouroValidator(v); err != nil {
			return &ValidationError{Name: "logradouro", err: fmt.Errorf(`ent: validator failed for field "Cep.logradouro": %w`, err)}
		}
	}
	if _, ok := cc.mutation.Bairro(); !ok {
		return &ValidationError{Name: "bairro", err: errors.New(`ent: missing required field "Cep.bairro"`)}
	}
	if v, ok := cc.mutation.Bairro(); ok {
		if err := cep.BairroValidator(v); err != nil {
			return &ValidationError{Name: "bairro", err: fmt.Errorf(`ent: validator failed for field "Cep.bairro": %w`, err)}
		}
	}
	if v, ok := cc.mutation.ID(); ok {
		if err := cep.IDValidator(v); err != nil {
			return &ValidationError{Name: "id", err: fmt.Errorf(`ent: validator failed for field "Cep.id": %w`, err)}
		}
	}
	if _, ok := cc.mutation.CidadeID(); !ok {
		return &ValidationError{Name: "cidade", err: errors.New(`ent: missing required edge "Cep.cidade"`)}
	}
	return nil
}

func (cc *CepCreate) sqlSave(ctx context.Context) (*Cep, error) {
	_node, _spec := cc.createSpec()
	if err := sqlgraph.CreateNode(ctx, cc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{err.Error(), err}
		}
		return nil, err
	}
	if _spec.ID.Value != _node.ID {
		id := _spec.ID.Value.(int64)
		_node.ID = int32(id)
	}
	return _node, nil
}

func (cc *CepCreate) createSpec() (*Cep, *sqlgraph.CreateSpec) {
	var (
		_node = &Cep{config: cc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: cep.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt32,
				Column: cep.FieldID,
			},
		}
	)
	if id, ok := cc.mutation.ID(); ok {
		_node.ID = id
		_spec.ID.Value = id
	}
	if value, ok := cc.mutation.Logradouro(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: cep.FieldLogradouro,
		})
		_node.Logradouro = value
	}
	if value, ok := cc.mutation.Bairro(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: cep.FieldBairro,
		})
		_node.Bairro = value
	}
	if nodes := cc.mutation.CidadeIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   cep.CidadeTable,
			Columns: []string{cep.CidadeColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: cidade.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.cidade_ceps = &nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	if nodes := cc.mutation.EnderecosIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   cep.EnderecosTable,
			Columns: []string{cep.EnderecosColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: endereco.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// CepCreateBulk is the builder for creating many Cep entities in bulk.
type CepCreateBulk struct {
	config
	builders []*CepCreate
}

// Save creates the Cep entities in the database.
func (ccb *CepCreateBulk) Save(ctx context.Context) ([]*Cep, error) {
	specs := make([]*sqlgraph.CreateSpec, len(ccb.builders))
	nodes := make([]*Cep, len(ccb.builders))
	mutators := make([]Mutator, len(ccb.builders))
	for i := range ccb.builders {
		func(i int, root context.Context) {
			builder := ccb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*CepMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, ccb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, ccb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{err.Error(), err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				mutation.done = true
				if specs[i].ID.Value != nil && nodes[i].ID == 0 {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int32(id)
				}
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, ccb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (ccb *CepCreateBulk) SaveX(ctx context.Context) []*Cep {
	v, err := ccb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (ccb *CepCreateBulk) Exec(ctx context.Context) error {
	_, err := ccb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (ccb *CepCreateBulk) ExecX(ctx context.Context) {
	if err := ccb.Exec(ctx); err != nil {
		panic(err)
	}
}
