// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"log"

	"unidade/ent/migrate"

	"unidade/ent/unidade"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
)

// Client is the client that holds all ent builders.
type Client struct {
	config
	// Schema is the client for creating, migrating and dropping schema.
	Schema *migrate.Schema
	// Unidade is the client for interacting with the Unidade builders.
	Unidade *UnidadeClient
}

// NewClient creates a new client configured with the given options.
func NewClient(opts ...Option) *Client {
	cfg := config{log: log.Println, hooks: &hooks{}}
	cfg.options(opts...)
	client := &Client{config: cfg}
	client.init()
	return client
}

func (c *Client) init() {
	c.Schema = migrate.NewSchema(c.driver)
	c.Unidade = NewUnidadeClient(c.config)
}

// Open opens a database/sql.DB specified by the driver name and
// the data source name, and returns a new client attached to it.
// Optional parameters can be added for configuring the client.
func Open(driverName, dataSourceName string, options ...Option) (*Client, error) {
	switch driverName {
	case dialect.MySQL, dialect.Postgres, dialect.SQLite:
		drv, err := sql.Open(driverName, dataSourceName)
		if err != nil {
			return nil, err
		}
		return NewClient(append(options, Driver(drv))...), nil
	default:
		return nil, fmt.Errorf("unsupported driver: %q", driverName)
	}
}

// Tx returns a new transactional client. The provided context
// is used until the transaction is committed or rolled back.
func (c *Client) Tx(ctx context.Context) (*Tx, error) {
	if _, ok := c.driver.(*txDriver); ok {
		return nil, fmt.Errorf("ent: cannot start a transaction within a transaction")
	}
	tx, err := newTx(ctx, c.driver)
	if err != nil {
		return nil, fmt.Errorf("ent: starting a transaction: %w", err)
	}
	cfg := c.config
	cfg.driver = tx
	return &Tx{
		ctx:     ctx,
		config:  cfg,
		Unidade: NewUnidadeClient(cfg),
	}, nil
}

// BeginTx returns a transactional client with specified options.
func (c *Client) BeginTx(ctx context.Context, opts *sql.TxOptions) (*Tx, error) {
	if _, ok := c.driver.(*txDriver); ok {
		return nil, fmt.Errorf("ent: cannot start a transaction within a transaction")
	}
	tx, err := c.driver.(interface {
		BeginTx(context.Context, *sql.TxOptions) (dialect.Tx, error)
	}).BeginTx(ctx, opts)
	if err != nil {
		return nil, fmt.Errorf("ent: starting a transaction: %w", err)
	}
	cfg := c.config
	cfg.driver = &txDriver{tx: tx, drv: c.driver}
	return &Tx{
		ctx:     ctx,
		config:  cfg,
		Unidade: NewUnidadeClient(cfg),
	}, nil
}

// Debug returns a new debug-client. It's used to get verbose logging on specific operations.
//
//	client.Debug().
//		Unidade.
//		Query().
//		Count(ctx)
//
func (c *Client) Debug() *Client {
	if c.debug {
		return c
	}
	cfg := c.config
	cfg.driver = dialect.Debug(c.driver, c.log)
	client := &Client{config: cfg}
	client.init()
	return client
}

// Close closes the database connection and prevents new queries from starting.
func (c *Client) Close() error {
	return c.driver.Close()
}

// Use adds the mutation hooks to all the entity clients.
// In order to add hooks to a specific client, call: `client.Node.Use(...)`.
func (c *Client) Use(hooks ...Hook) {
	c.Unidade.Use(hooks...)
}

// UnidadeClient is a client for the Unidade schema.
type UnidadeClient struct {
	config
}

// NewUnidadeClient returns a client for the Unidade from the given config.
func NewUnidadeClient(c config) *UnidadeClient {
	return &UnidadeClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `unidade.Hooks(f(g(h())))`.
func (c *UnidadeClient) Use(hooks ...Hook) {
	c.hooks.Unidade = append(c.hooks.Unidade, hooks...)
}

// Create returns a create builder for Unidade.
func (c *UnidadeClient) Create() *UnidadeCreate {
	mutation := newUnidadeMutation(c.config, OpCreate)
	return &UnidadeCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of Unidade entities.
func (c *UnidadeClient) CreateBulk(builders ...*UnidadeCreate) *UnidadeCreateBulk {
	return &UnidadeCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for Unidade.
func (c *UnidadeClient) Update() *UnidadeUpdate {
	mutation := newUnidadeMutation(c.config, OpUpdate)
	return &UnidadeUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *UnidadeClient) UpdateOne(u *Unidade) *UnidadeUpdateOne {
	mutation := newUnidadeMutation(c.config, OpUpdateOne, withUnidade(u))
	return &UnidadeUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *UnidadeClient) UpdateOneID(id int) *UnidadeUpdateOne {
	mutation := newUnidadeMutation(c.config, OpUpdateOne, withUnidadeID(id))
	return &UnidadeUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for Unidade.
func (c *UnidadeClient) Delete() *UnidadeDelete {
	mutation := newUnidadeMutation(c.config, OpDelete)
	return &UnidadeDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a delete builder for the given entity.
func (c *UnidadeClient) DeleteOne(u *Unidade) *UnidadeDeleteOne {
	return c.DeleteOneID(u.ID)
}

// DeleteOneID returns a delete builder for the given id.
func (c *UnidadeClient) DeleteOneID(id int) *UnidadeDeleteOne {
	builder := c.Delete().Where(unidade.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &UnidadeDeleteOne{builder}
}

// Query returns a query builder for Unidade.
func (c *UnidadeClient) Query() *UnidadeQuery {
	return &UnidadeQuery{
		config: c.config,
	}
}

// Get returns a Unidade entity by its id.
func (c *UnidadeClient) Get(ctx context.Context, id int) (*Unidade, error) {
	return c.Query().Where(unidade.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *UnidadeClient) GetX(ctx context.Context, id int) *Unidade {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *UnidadeClient) Hooks() []Hook {
	return c.hooks.Unidade
}
