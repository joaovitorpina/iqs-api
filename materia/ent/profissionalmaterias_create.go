// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"materia/ent/materia"
	"materia/ent/profissionalmaterias"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ProfissionalMateriasCreate is the builder for creating a ProfissionalMaterias entity.
type ProfissionalMateriasCreate struct {
	config
	mutation *ProfissionalMateriasMutation
	hooks    []Hook
}

// SetID sets the "id" field.
func (pmc *ProfissionalMateriasCreate) SetID(i int) *ProfissionalMateriasCreate {
	pmc.mutation.SetID(i)
	return pmc
}

// SetMateriaID sets the "materia" edge to the Materia entity by ID.
func (pmc *ProfissionalMateriasCreate) SetMateriaID(id int) *ProfissionalMateriasCreate {
	pmc.mutation.SetMateriaID(id)
	return pmc
}

// SetMateria sets the "materia" edge to the Materia entity.
func (pmc *ProfissionalMateriasCreate) SetMateria(m *Materia) *ProfissionalMateriasCreate {
	return pmc.SetMateriaID(m.ID)
}

// Mutation returns the ProfissionalMateriasMutation object of the builder.
func (pmc *ProfissionalMateriasCreate) Mutation() *ProfissionalMateriasMutation {
	return pmc.mutation
}

// Save creates the ProfissionalMaterias in the database.
func (pmc *ProfissionalMateriasCreate) Save(ctx context.Context) (*ProfissionalMaterias, error) {
	var (
		err  error
		node *ProfissionalMaterias
	)
	if len(pmc.hooks) == 0 {
		if err = pmc.check(); err != nil {
			return nil, err
		}
		node, err = pmc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*ProfissionalMateriasMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = pmc.check(); err != nil {
				return nil, err
			}
			pmc.mutation = mutation
			if node, err = pmc.sqlSave(ctx); err != nil {
				return nil, err
			}
			mutation.id = &node.ID
			mutation.done = true
			return node, err
		})
		for i := len(pmc.hooks) - 1; i >= 0; i-- {
			if pmc.hooks[i] == nil {
				return nil, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = pmc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, pmc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (pmc *ProfissionalMateriasCreate) SaveX(ctx context.Context) *ProfissionalMaterias {
	v, err := pmc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (pmc *ProfissionalMateriasCreate) Exec(ctx context.Context) error {
	_, err := pmc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (pmc *ProfissionalMateriasCreate) ExecX(ctx context.Context) {
	if err := pmc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (pmc *ProfissionalMateriasCreate) check() error {
	if v, ok := pmc.mutation.ID(); ok {
		if err := profissionalmaterias.IDValidator(v); err != nil {
			return &ValidationError{Name: "id", err: fmt.Errorf(`ent: validator failed for field "ProfissionalMaterias.id": %w`, err)}
		}
	}
	if _, ok := pmc.mutation.MateriaID(); !ok {
		return &ValidationError{Name: "materia", err: errors.New(`ent: missing required edge "ProfissionalMaterias.materia"`)}
	}
	return nil
}

func (pmc *ProfissionalMateriasCreate) sqlSave(ctx context.Context) (*ProfissionalMaterias, error) {
	_node, _spec := pmc.createSpec()
	if err := sqlgraph.CreateNode(ctx, pmc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{err.Error(), err}
		}
		return nil, err
	}
	if _spec.ID.Value != _node.ID {
		id := _spec.ID.Value.(int64)
		_node.ID = int(id)
	}
	return _node, nil
}

func (pmc *ProfissionalMateriasCreate) createSpec() (*ProfissionalMaterias, *sqlgraph.CreateSpec) {
	var (
		_node = &ProfissionalMaterias{config: pmc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: profissionalmaterias.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: profissionalmaterias.FieldID,
			},
		}
	)
	if id, ok := pmc.mutation.ID(); ok {
		_node.ID = id
		_spec.ID.Value = id
	}
	if nodes := pmc.mutation.MateriaIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   profissionalmaterias.MateriaTable,
			Columns: []string{profissionalmaterias.MateriaColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: materia.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.materia_profissional_materias = &nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// ProfissionalMateriasCreateBulk is the builder for creating many ProfissionalMaterias entities in bulk.
type ProfissionalMateriasCreateBulk struct {
	config
	builders []*ProfissionalMateriasCreate
}

// Save creates the ProfissionalMaterias entities in the database.
func (pmcb *ProfissionalMateriasCreateBulk) Save(ctx context.Context) ([]*ProfissionalMaterias, error) {
	specs := make([]*sqlgraph.CreateSpec, len(pmcb.builders))
	nodes := make([]*ProfissionalMaterias, len(pmcb.builders))
	mutators := make([]Mutator, len(pmcb.builders))
	for i := range pmcb.builders {
		func(i int, root context.Context) {
			builder := pmcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*ProfissionalMateriasMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, pmcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, pmcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{err.Error(), err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				mutation.done = true
				if specs[i].ID.Value != nil && nodes[i].ID == 0 {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, pmcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (pmcb *ProfissionalMateriasCreateBulk) SaveX(ctx context.Context) []*ProfissionalMaterias {
	v, err := pmcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (pmcb *ProfissionalMateriasCreateBulk) Exec(ctx context.Context) error {
	_, err := pmcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (pmcb *ProfissionalMateriasCreateBulk) ExecX(ctx context.Context) {
	if err := pmcb.Exec(ctx); err != nil {
		panic(err)
	}
}
