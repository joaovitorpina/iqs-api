// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"profissional/ent/convenio"
	"profissional/ent/profissional"
	"strings"

	"entgo.io/ent/dialect/sql"
)

// Convenio is the model entity for the Convenio schema.
type Convenio struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Nome holds the value of the "nome" field.
	Nome string `json:"nome,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ConvenioQuery when eager-loading is set.
	Edges                  ConvenioEdges `json:"edges"`
	profissional_convenios *int
}

// ConvenioEdges holds the relations/edges for other nodes in the graph.
type ConvenioEdges struct {
	// Profissional holds the value of the profissional edge.
	Profissional *Profissional `json:"profissional,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// ProfissionalOrErr returns the Profissional value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ConvenioEdges) ProfissionalOrErr() (*Profissional, error) {
	if e.loadedTypes[0] {
		if e.Profissional == nil {
			// The edge profissional was loaded in eager-loading,
			// but was not found.
			return nil, &NotFoundError{label: profissional.Label}
		}
		return e.Profissional, nil
	}
	return nil, &NotLoadedError{edge: "profissional"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Convenio) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case convenio.FieldID:
			values[i] = new(sql.NullInt64)
		case convenio.FieldNome:
			values[i] = new(sql.NullString)
		case convenio.ForeignKeys[0]: // profissional_convenios
			values[i] = new(sql.NullInt64)
		default:
			return nil, fmt.Errorf("unexpected column %q for type Convenio", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Convenio fields.
func (c *Convenio) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case convenio.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			c.ID = int(value.Int64)
		case convenio.FieldNome:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field nome", values[i])
			} else if value.Valid {
				c.Nome = value.String
			}
		case convenio.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field profissional_convenios", value)
			} else if value.Valid {
				c.profissional_convenios = new(int)
				*c.profissional_convenios = int(value.Int64)
			}
		}
	}
	return nil
}

// QueryProfissional queries the "profissional" edge of the Convenio entity.
func (c *Convenio) QueryProfissional() *ProfissionalQuery {
	return (&ConvenioClient{config: c.config}).QueryProfissional(c)
}

// Update returns a builder for updating this Convenio.
// Note that you need to call Convenio.Unwrap() before calling this method if this Convenio
// was returned from a transaction, and the transaction was committed or rolled back.
func (c *Convenio) Update() *ConvenioUpdateOne {
	return (&ConvenioClient{config: c.config}).UpdateOne(c)
}

// Unwrap unwraps the Convenio entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (c *Convenio) Unwrap() *Convenio {
	tx, ok := c.config.driver.(*txDriver)
	if !ok {
		panic("ent: Convenio is not a transactional entity")
	}
	c.config.driver = tx.drv
	return c
}

// String implements the fmt.Stringer.
func (c *Convenio) String() string {
	var builder strings.Builder
	builder.WriteString("Convenio(")
	builder.WriteString(fmt.Sprintf("id=%v", c.ID))
	builder.WriteString(", nome=")
	builder.WriteString(c.Nome)
	builder.WriteByte(')')
	return builder.String()
}

// Convenios is a parsable slice of Convenio.
type Convenios []*Convenio

func (c Convenios) config(cfg config) {
	for _i := range c {
		c[_i].config = cfg
	}
}
