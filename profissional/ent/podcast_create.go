// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"profissional/ent/podcast"
	"profissional/ent/profissional"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// PodcastCreate is the builder for creating a Podcast entity.
type PodcastCreate struct {
	config
	mutation *PodcastMutation
	hooks    []Hook
}

// SetTitulo sets the "titulo" field.
func (pc *PodcastCreate) SetTitulo(s string) *PodcastCreate {
	pc.mutation.SetTitulo(s)
	return pc
}

// SetCodigo sets the "codigo" field.
func (pc *PodcastCreate) SetCodigo(s string) *PodcastCreate {
	pc.mutation.SetCodigo(s)
	return pc
}

// SetProfissionalID sets the "profissional" edge to the Profissional entity by ID.
func (pc *PodcastCreate) SetProfissionalID(id int) *PodcastCreate {
	pc.mutation.SetProfissionalID(id)
	return pc
}

// SetProfissional sets the "profissional" edge to the Profissional entity.
func (pc *PodcastCreate) SetProfissional(p *Profissional) *PodcastCreate {
	return pc.SetProfissionalID(p.ID)
}

// Mutation returns the PodcastMutation object of the builder.
func (pc *PodcastCreate) Mutation() *PodcastMutation {
	return pc.mutation
}

// Save creates the Podcast in the database.
func (pc *PodcastCreate) Save(ctx context.Context) (*Podcast, error) {
	var (
		err  error
		node *Podcast
	)
	if len(pc.hooks) == 0 {
		if err = pc.check(); err != nil {
			return nil, err
		}
		node, err = pc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*PodcastMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = pc.check(); err != nil {
				return nil, err
			}
			pc.mutation = mutation
			if node, err = pc.sqlSave(ctx); err != nil {
				return nil, err
			}
			mutation.id = &node.ID
			mutation.done = true
			return node, err
		})
		for i := len(pc.hooks) - 1; i >= 0; i-- {
			if pc.hooks[i] == nil {
				return nil, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = pc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, pc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (pc *PodcastCreate) SaveX(ctx context.Context) *Podcast {
	v, err := pc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (pc *PodcastCreate) Exec(ctx context.Context) error {
	_, err := pc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (pc *PodcastCreate) ExecX(ctx context.Context) {
	if err := pc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (pc *PodcastCreate) check() error {
	if _, ok := pc.mutation.Titulo(); !ok {
		return &ValidationError{Name: "titulo", err: errors.New(`ent: missing required field "Podcast.titulo"`)}
	}
	if _, ok := pc.mutation.Codigo(); !ok {
		return &ValidationError{Name: "codigo", err: errors.New(`ent: missing required field "Podcast.codigo"`)}
	}
	if _, ok := pc.mutation.ProfissionalID(); !ok {
		return &ValidationError{Name: "profissional", err: errors.New(`ent: missing required edge "Podcast.profissional"`)}
	}
	return nil
}

func (pc *PodcastCreate) sqlSave(ctx context.Context) (*Podcast, error) {
	_node, _spec := pc.createSpec()
	if err := sqlgraph.CreateNode(ctx, pc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{err.Error(), err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (pc *PodcastCreate) createSpec() (*Podcast, *sqlgraph.CreateSpec) {
	var (
		_node = &Podcast{config: pc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: podcast.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: podcast.FieldID,
			},
		}
	)
	if value, ok := pc.mutation.Titulo(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: podcast.FieldTitulo,
		})
		_node.Titulo = value
	}
	if value, ok := pc.mutation.Codigo(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: podcast.FieldCodigo,
		})
		_node.Codigo = value
	}
	if nodes := pc.mutation.ProfissionalIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   podcast.ProfissionalTable,
			Columns: []string{podcast.ProfissionalColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: profissional.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.profissional_podcasts = &nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// PodcastCreateBulk is the builder for creating many Podcast entities in bulk.
type PodcastCreateBulk struct {
	config
	builders []*PodcastCreate
}

// Save creates the Podcast entities in the database.
func (pcb *PodcastCreateBulk) Save(ctx context.Context) ([]*Podcast, error) {
	specs := make([]*sqlgraph.CreateSpec, len(pcb.builders))
	nodes := make([]*Podcast, len(pcb.builders))
	mutators := make([]Mutator, len(pcb.builders))
	for i := range pcb.builders {
		func(i int, root context.Context) {
			builder := pcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*PodcastMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, pcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, pcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{err.Error(), err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				mutation.done = true
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, pcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (pcb *PodcastCreateBulk) SaveX(ctx context.Context) []*Podcast {
	v, err := pcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (pcb *PodcastCreateBulk) Exec(ctx context.Context) error {
	_, err := pcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (pcb *PodcastCreateBulk) ExecX(ctx context.Context) {
	if err := pcb.Exec(ctx); err != nil {
		panic(err)
	}
}
