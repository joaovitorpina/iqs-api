// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"profissional/ent/podcast"
	"profissional/ent/predicate"
	"profissional/ent/profissional"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// PodcastUpdate is the builder for updating Podcast entities.
type PodcastUpdate struct {
	config
	hooks    []Hook
	mutation *PodcastMutation
}

// Where appends a list predicates to the PodcastUpdate builder.
func (pu *PodcastUpdate) Where(ps ...predicate.Podcast) *PodcastUpdate {
	pu.mutation.Where(ps...)
	return pu
}

// SetTitulo sets the "titulo" field.
func (pu *PodcastUpdate) SetTitulo(s string) *PodcastUpdate {
	pu.mutation.SetTitulo(s)
	return pu
}

// SetCodigo sets the "codigo" field.
func (pu *PodcastUpdate) SetCodigo(s string) *PodcastUpdate {
	pu.mutation.SetCodigo(s)
	return pu
}

// SetProfissionalID sets the "profissional" edge to the Profissional entity by ID.
func (pu *PodcastUpdate) SetProfissionalID(id int) *PodcastUpdate {
	pu.mutation.SetProfissionalID(id)
	return pu
}

// SetNillableProfissionalID sets the "profissional" edge to the Profissional entity by ID if the given value is not nil.
func (pu *PodcastUpdate) SetNillableProfissionalID(id *int) *PodcastUpdate {
	if id != nil {
		pu = pu.SetProfissionalID(*id)
	}
	return pu
}

// SetProfissional sets the "profissional" edge to the Profissional entity.
func (pu *PodcastUpdate) SetProfissional(p *Profissional) *PodcastUpdate {
	return pu.SetProfissionalID(p.ID)
}

// Mutation returns the PodcastMutation object of the builder.
func (pu *PodcastUpdate) Mutation() *PodcastMutation {
	return pu.mutation
}

// ClearProfissional clears the "profissional" edge to the Profissional entity.
func (pu *PodcastUpdate) ClearProfissional() *PodcastUpdate {
	pu.mutation.ClearProfissional()
	return pu
}

// Save executes the query and returns the number of nodes affected by the update operation.
func (pu *PodcastUpdate) Save(ctx context.Context) (int, error) {
	var (
		err      error
		affected int
	)
	if len(pu.hooks) == 0 {
		affected, err = pu.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*PodcastMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			pu.mutation = mutation
			affected, err = pu.sqlSave(ctx)
			mutation.done = true
			return affected, err
		})
		for i := len(pu.hooks) - 1; i >= 0; i-- {
			if pu.hooks[i] == nil {
				return 0, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = pu.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, pu.mutation); err != nil {
			return 0, err
		}
	}
	return affected, err
}

// SaveX is like Save, but panics if an error occurs.
func (pu *PodcastUpdate) SaveX(ctx context.Context) int {
	affected, err := pu.Save(ctx)
	if err != nil {
		panic(err)
	}
	return affected
}

// Exec executes the query.
func (pu *PodcastUpdate) Exec(ctx context.Context) error {
	_, err := pu.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (pu *PodcastUpdate) ExecX(ctx context.Context) {
	if err := pu.Exec(ctx); err != nil {
		panic(err)
	}
}

func (pu *PodcastUpdate) sqlSave(ctx context.Context) (n int, err error) {
	_spec := &sqlgraph.UpdateSpec{
		Node: &sqlgraph.NodeSpec{
			Table:   podcast.Table,
			Columns: podcast.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: podcast.FieldID,
			},
		},
	}
	if ps := pu.mutation.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if value, ok := pu.mutation.Titulo(); ok {
		_spec.Fields.Set = append(_spec.Fields.Set, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: podcast.FieldTitulo,
		})
	}
	if value, ok := pu.mutation.Codigo(); ok {
		_spec.Fields.Set = append(_spec.Fields.Set, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: podcast.FieldCodigo,
		})
	}
	if pu.mutation.ProfissionalCleared() {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   podcast.ProfissionalTable,
			Columns: []string{podcast.ProfissionalColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: profissional.FieldID,
				},
			},
		}
		_spec.Edges.Clear = append(_spec.Edges.Clear, edge)
	}
	if nodes := pu.mutation.ProfissionalIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   podcast.ProfissionalTable,
			Columns: []string{podcast.ProfissionalColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: profissional.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges.Add = append(_spec.Edges.Add, edge)
	}
	if n, err = sqlgraph.UpdateNodes(ctx, pu.driver, _spec); err != nil {
		if _, ok := err.(*sqlgraph.NotFoundError); ok {
			err = &NotFoundError{podcast.Label}
		} else if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{err.Error(), err}
		}
		return 0, err
	}
	return n, nil
}

// PodcastUpdateOne is the builder for updating a single Podcast entity.
type PodcastUpdateOne struct {
	config
	fields   []string
	hooks    []Hook
	mutation *PodcastMutation
}

// SetTitulo sets the "titulo" field.
func (puo *PodcastUpdateOne) SetTitulo(s string) *PodcastUpdateOne {
	puo.mutation.SetTitulo(s)
	return puo
}

// SetCodigo sets the "codigo" field.
func (puo *PodcastUpdateOne) SetCodigo(s string) *PodcastUpdateOne {
	puo.mutation.SetCodigo(s)
	return puo
}

// SetProfissionalID sets the "profissional" edge to the Profissional entity by ID.
func (puo *PodcastUpdateOne) SetProfissionalID(id int) *PodcastUpdateOne {
	puo.mutation.SetProfissionalID(id)
	return puo
}

// SetNillableProfissionalID sets the "profissional" edge to the Profissional entity by ID if the given value is not nil.
func (puo *PodcastUpdateOne) SetNillableProfissionalID(id *int) *PodcastUpdateOne {
	if id != nil {
		puo = puo.SetProfissionalID(*id)
	}
	return puo
}

// SetProfissional sets the "profissional" edge to the Profissional entity.
func (puo *PodcastUpdateOne) SetProfissional(p *Profissional) *PodcastUpdateOne {
	return puo.SetProfissionalID(p.ID)
}

// Mutation returns the PodcastMutation object of the builder.
func (puo *PodcastUpdateOne) Mutation() *PodcastMutation {
	return puo.mutation
}

// ClearProfissional clears the "profissional" edge to the Profissional entity.
func (puo *PodcastUpdateOne) ClearProfissional() *PodcastUpdateOne {
	puo.mutation.ClearProfissional()
	return puo
}

// Select allows selecting one or more fields (columns) of the returned entity.
// The default is selecting all fields defined in the entity schema.
func (puo *PodcastUpdateOne) Select(field string, fields ...string) *PodcastUpdateOne {
	puo.fields = append([]string{field}, fields...)
	return puo
}

// Save executes the query and returns the updated Podcast entity.
func (puo *PodcastUpdateOne) Save(ctx context.Context) (*Podcast, error) {
	var (
		err  error
		node *Podcast
	)
	if len(puo.hooks) == 0 {
		node, err = puo.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*PodcastMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			puo.mutation = mutation
			node, err = puo.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(puo.hooks) - 1; i >= 0; i-- {
			if puo.hooks[i] == nil {
				return nil, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = puo.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, puo.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX is like Save, but panics if an error occurs.
func (puo *PodcastUpdateOne) SaveX(ctx context.Context) *Podcast {
	node, err := puo.Save(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// Exec executes the query on the entity.
func (puo *PodcastUpdateOne) Exec(ctx context.Context) error {
	_, err := puo.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (puo *PodcastUpdateOne) ExecX(ctx context.Context) {
	if err := puo.Exec(ctx); err != nil {
		panic(err)
	}
}

func (puo *PodcastUpdateOne) sqlSave(ctx context.Context) (_node *Podcast, err error) {
	_spec := &sqlgraph.UpdateSpec{
		Node: &sqlgraph.NodeSpec{
			Table:   podcast.Table,
			Columns: podcast.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: podcast.FieldID,
			},
		},
	}
	id, ok := puo.mutation.ID()
	if !ok {
		return nil, &ValidationError{Name: "id", err: errors.New(`ent: missing "Podcast.id" for update`)}
	}
	_spec.Node.ID.Value = id
	if fields := puo.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, podcast.FieldID)
		for _, f := range fields {
			if !podcast.ValidColumn(f) {
				return nil, &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
			}
			if f != podcast.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, f)
			}
		}
	}
	if ps := puo.mutation.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if value, ok := puo.mutation.Titulo(); ok {
		_spec.Fields.Set = append(_spec.Fields.Set, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: podcast.FieldTitulo,
		})
	}
	if value, ok := puo.mutation.Codigo(); ok {
		_spec.Fields.Set = append(_spec.Fields.Set, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: podcast.FieldCodigo,
		})
	}
	if puo.mutation.ProfissionalCleared() {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   podcast.ProfissionalTable,
			Columns: []string{podcast.ProfissionalColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: profissional.FieldID,
				},
			},
		}
		_spec.Edges.Clear = append(_spec.Edges.Clear, edge)
	}
	if nodes := puo.mutation.ProfissionalIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   podcast.ProfissionalTable,
			Columns: []string{podcast.ProfissionalColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: profissional.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges.Add = append(_spec.Edges.Add, edge)
	}
	_node = &Podcast{config: puo.config}
	_spec.Assign = _node.assignValues
	_spec.ScanValues = _node.scanValues
	if err = sqlgraph.UpdateNode(ctx, puo.driver, _spec); err != nil {
		if _, ok := err.(*sqlgraph.NotFoundError); ok {
			err = &NotFoundError{podcast.Label}
		} else if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{err.Error(), err}
		}
		return nil, err
	}
	return _node, nil
}
