// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"profissional/ent/foto"
	"profissional/ent/profissional"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// FotoCreate is the builder for creating a Foto entity.
type FotoCreate struct {
	config
	mutation *FotoMutation
	hooks    []Hook
}

// SetTitulo sets the "titulo" field.
func (fc *FotoCreate) SetTitulo(s string) *FotoCreate {
	fc.mutation.SetTitulo(s)
	return fc
}

// SetURL sets the "url" field.
func (fc *FotoCreate) SetURL(s string) *FotoCreate {
	fc.mutation.SetURL(s)
	return fc
}

// SetProfissionalID sets the "profissional" edge to the Profissional entity by ID.
func (fc *FotoCreate) SetProfissionalID(id int) *FotoCreate {
	fc.mutation.SetProfissionalID(id)
	return fc
}

// SetProfissional sets the "profissional" edge to the Profissional entity.
func (fc *FotoCreate) SetProfissional(p *Profissional) *FotoCreate {
	return fc.SetProfissionalID(p.ID)
}

// Mutation returns the FotoMutation object of the builder.
func (fc *FotoCreate) Mutation() *FotoMutation {
	return fc.mutation
}

// Save creates the Foto in the database.
func (fc *FotoCreate) Save(ctx context.Context) (*Foto, error) {
	var (
		err  error
		node *Foto
	)
	if len(fc.hooks) == 0 {
		if err = fc.check(); err != nil {
			return nil, err
		}
		node, err = fc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*FotoMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = fc.check(); err != nil {
				return nil, err
			}
			fc.mutation = mutation
			if node, err = fc.sqlSave(ctx); err != nil {
				return nil, err
			}
			mutation.id = &node.ID
			mutation.done = true
			return node, err
		})
		for i := len(fc.hooks) - 1; i >= 0; i-- {
			if fc.hooks[i] == nil {
				return nil, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = fc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, fc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (fc *FotoCreate) SaveX(ctx context.Context) *Foto {
	v, err := fc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (fc *FotoCreate) Exec(ctx context.Context) error {
	_, err := fc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (fc *FotoCreate) ExecX(ctx context.Context) {
	if err := fc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (fc *FotoCreate) check() error {
	if _, ok := fc.mutation.Titulo(); !ok {
		return &ValidationError{Name: "titulo", err: errors.New(`ent: missing required field "Foto.titulo"`)}
	}
	if _, ok := fc.mutation.URL(); !ok {
		return &ValidationError{Name: "url", err: errors.New(`ent: missing required field "Foto.url"`)}
	}
	if _, ok := fc.mutation.ProfissionalID(); !ok {
		return &ValidationError{Name: "profissional", err: errors.New(`ent: missing required edge "Foto.profissional"`)}
	}
	return nil
}

func (fc *FotoCreate) sqlSave(ctx context.Context) (*Foto, error) {
	_node, _spec := fc.createSpec()
	if err := sqlgraph.CreateNode(ctx, fc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{err.Error(), err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (fc *FotoCreate) createSpec() (*Foto, *sqlgraph.CreateSpec) {
	var (
		_node = &Foto{config: fc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: foto.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: foto.FieldID,
			},
		}
	)
	if value, ok := fc.mutation.Titulo(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: foto.FieldTitulo,
		})
		_node.Titulo = value
	}
	if value, ok := fc.mutation.URL(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: foto.FieldURL,
		})
		_node.URL = value
	}
	if nodes := fc.mutation.ProfissionalIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   foto.ProfissionalTable,
			Columns: []string{foto.ProfissionalColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: profissional.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.profissional_fotos = &nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// FotoCreateBulk is the builder for creating many Foto entities in bulk.
type FotoCreateBulk struct {
	config
	builders []*FotoCreate
}

// Save creates the Foto entities in the database.
func (fcb *FotoCreateBulk) Save(ctx context.Context) ([]*Foto, error) {
	specs := make([]*sqlgraph.CreateSpec, len(fcb.builders))
	nodes := make([]*Foto, len(fcb.builders))
	mutators := make([]Mutator, len(fcb.builders))
	for i := range fcb.builders {
		func(i int, root context.Context) {
			builder := fcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*FotoMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, fcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, fcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{err.Error(), err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				mutation.done = true
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, fcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (fcb *FotoCreateBulk) SaveX(ctx context.Context) []*Foto {
	v, err := fcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (fcb *FotoCreateBulk) Exec(ctx context.Context) error {
	_, err := fcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (fcb *FotoCreateBulk) ExecX(ctx context.Context) {
	if err := fcb.Exec(ctx); err != nil {
		panic(err)
	}
}
