// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"profissional/ent/profissional"
	"profissional/ent/tratamento"
	"strings"

	"entgo.io/ent/dialect/sql"
)

// Tratamento is the model entity for the Tratamento schema.
type Tratamento struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Descricao holds the value of the "descricao" field.
	Descricao string `json:"descricao,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the TratamentoQuery when eager-loading is set.
	Edges                    TratamentoEdges `json:"edges"`
	profissional_tratamentos *int
}

// TratamentoEdges holds the relations/edges for other nodes in the graph.
type TratamentoEdges struct {
	// Profissional holds the value of the profissional edge.
	Profissional *Profissional `json:"profissional,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// ProfissionalOrErr returns the Profissional value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e TratamentoEdges) ProfissionalOrErr() (*Profissional, error) {
	if e.loadedTypes[0] {
		if e.Profissional == nil {
			// The edge profissional was loaded in eager-loading,
			// but was not found.
			return nil, &NotFoundError{label: profissional.Label}
		}
		return e.Profissional, nil
	}
	return nil, &NotLoadedError{edge: "profissional"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Tratamento) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case tratamento.FieldID:
			values[i] = new(sql.NullInt64)
		case tratamento.FieldDescricao:
			values[i] = new(sql.NullString)
		case tratamento.ForeignKeys[0]: // profissional_tratamentos
			values[i] = new(sql.NullInt64)
		default:
			return nil, fmt.Errorf("unexpected column %q for type Tratamento", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Tratamento fields.
func (t *Tratamento) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case tratamento.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			t.ID = int(value.Int64)
		case tratamento.FieldDescricao:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field descricao", values[i])
			} else if value.Valid {
				t.Descricao = value.String
			}
		case tratamento.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field profissional_tratamentos", value)
			} else if value.Valid {
				t.profissional_tratamentos = new(int)
				*t.profissional_tratamentos = int(value.Int64)
			}
		}
	}
	return nil
}

// QueryProfissional queries the "profissional" edge of the Tratamento entity.
func (t *Tratamento) QueryProfissional() *ProfissionalQuery {
	return (&TratamentoClient{config: t.config}).QueryProfissional(t)
}

// Update returns a builder for updating this Tratamento.
// Note that you need to call Tratamento.Unwrap() before calling this method if this Tratamento
// was returned from a transaction, and the transaction was committed or rolled back.
func (t *Tratamento) Update() *TratamentoUpdateOne {
	return (&TratamentoClient{config: t.config}).UpdateOne(t)
}

// Unwrap unwraps the Tratamento entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (t *Tratamento) Unwrap() *Tratamento {
	tx, ok := t.config.driver.(*txDriver)
	if !ok {
		panic("ent: Tratamento is not a transactional entity")
	}
	t.config.driver = tx.drv
	return t
}

// String implements the fmt.Stringer.
func (t *Tratamento) String() string {
	var builder strings.Builder
	builder.WriteString("Tratamento(")
	builder.WriteString(fmt.Sprintf("id=%v", t.ID))
	builder.WriteString(", descricao=")
	builder.WriteString(t.Descricao)
	builder.WriteByte(')')
	return builder.String()
}

// Tratamentos is a parsable slice of Tratamento.
type Tratamentos []*Tratamento

func (t Tratamentos) config(cfg config) {
	for _i := range t {
		t[_i].config = cfg
	}
}
